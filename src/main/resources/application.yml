server:
  error:
    include-stacktrace: never # Ocultando o stacktrace do erro no retorno da pagina

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring?useSSL=false&createDatabaseIfNotExists=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update